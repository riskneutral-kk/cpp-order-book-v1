#include <bits/stdc++.h>
#include <iostream>
using namespace std;

enum class Side {
    Buy,
    Sell
};

struct Order {
    // Price,Quantity,Buy or Sell, ID 
    int64_t id;
    uint16_t quantity;
    Side side;
    int16_t price;
};



int main(){
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    
    

    return 0;
}
